--- origsrc/mate-text-editor-1.2.0/pluma/Makefile.am	2012-03-11 14:49:10.000000000 -0500
+++ src/mate-text-editor-1.2.0/pluma/Makefile.am	2012-04-17 16:02:17.300624900 -0500
@@ -225,10 +225,11 @@ dist-hook:
 install-exec-hook:
 if PLATFORM_WIN32
 	$(mkinstalldirs) "$(DESTDIR)$(libdir)"
-	$(INSTALL_DATA) libpluma.a "$(DESTDIR)$(libdir)"
-else
+	$(INSTALL_DATA) libpluma.a "$(DESTDIR)$(libdir)/libpluma.dll.a"
+endif
+if !OS_WIN32
 	rm -f $(DESTDIR)$(bindir)/mate-text-editor
-	ln -s pluma $(DESTDIR)$(bindir)/mate-text-editor
+	ln -s pluma$(EXEEXT) $(DESTDIR)$(bindir)/mate-text-editor
 endif
 
 if !OS_WIN32
